{
  "name": "montreal-hospitality-website",
  "version": "6.0.0",
  "private": true,
  "description": "Montreal Hospitality static website. Built with Tailwind CSS via CLI, deployed to Cloudflare Pages.",
  "scripts": {
    "build": "tailwindcss -i ./src/input.css -o ./styles.css --minify",
    "watch": "tailwindcss -i ./src/input.css -o ./styles.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0"
  }
}
